name: Run unity test

# Toda vez que um usuário fizer push, a gente vai executar uma sequência de tarefas
on: [push]

jobs:
  run-unity-test:
    name: Run unity test
    runs-on: ubuntu-latest  # Vai executar os testes unitários da minha aplicação no ambiente Linux, a última versão
    
    services:
      postgres:
        image: bitnami/postgresql
        ports:
         - 5432:5432
        env:
          POSTGRESQL_USERNAME: mypstgresdb
          POSTGRESQL_PASSWORD: root
          POSTGRESQL_DATABASE: fluencyhub

    steps:
      - uses: actions/checkout@v3  # Vai baixar o código na máquina

      - uses: actions/setup-node@v3
        with:
          node-version: 22
          cache: 'npm'

      - run: npm ci

        env:
          NODE_ENV: dev
          PORT: 4000
          DATABASE_URL: "postgresql://fluency_user:57IzOzNTv2DC359sXsjbm9sB1hmq0iOw@dpg-crvu415ds78s738ciju0-a/fluency"
